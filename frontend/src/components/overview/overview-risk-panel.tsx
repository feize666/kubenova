import type { ReactNode } from "react";
export function OverviewRiskPanel({ title, scope, children, action }: { title: string; scope?: string; children: ReactNode; action?: ReactNode }) { return <section className="ops-overview-card ops-surface ops-surface--panel ops-surface--pad-none"><div className="ops-overview-card__header"><div className="ops-overview-card__heading"><div className="ops-overview-card__title">{title}</div>{scope ? <span className="ops-overview-card__scope">{scope}</span> : null}</div>{action}</div><div className="ops-overview-card__body">{children}</div></section>; }
